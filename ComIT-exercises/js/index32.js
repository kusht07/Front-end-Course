let height =  10;
let base = 4;

console.log(`The Perimeter of a rectangle with height = ${height} and base = ${base} is = ${2*(height+base)}.`);
console.log(`The Area of a rectangle with height = ${height} and base = ${base} is = ${height*base}.`);